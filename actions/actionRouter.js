// bring in express, and the actionModel
const express = require('express');
const actionModel = require('../data/helpers/actionModel');

// instantiate a router
const router = express.Router();

//export the router
module.exports = router;
