// bring in express, and the projectModel
const express = require('express');
const actionModel = require('../data/helpers/projectModel');

// instantiate a router
const router = express.Router();

//export the router
module.exports = router;
